# Python implementation

## Classes

- [x] RamObject
- [x] RamUser
- [x] RamState
- [x] RamFileType
- [ ] RamItem       (folderPath fini => à vérifier) Question : N° des Shots ??
- [-] RamShot 
- [-] RamAsset
- [-] RamStep (stepType if online et différencier shot/asset production)
- [ ] RamPipe
- [X] RamProject
- [ ] Ramses        (getStates : ok)

pour if online : le modèle est dans RamItem.folderPath()

## Refactor

- [ ] Voir si on peut remplace [R,G,B] pour les couleurs par une classe couleur de python (commune à python 2 et 3) (ou en créer une sinon ?)
=> J'ai trouvé : from matplotlib import pyplot
(package à installer)
Est-ce que ça peut aller ?

- [x] Remplacer `if not self._offline and self._daemon.online():` par `if not self._offline and self.connect():`

## Attention : currentStep (dans ramses.py) et getcurrentStep (dans ramStep.py) à revoir



## Duf

- daemon setCurrentProject doit renvoyer les memes infos que getCurrentProject

Ajouts démon :

- getShot, getAsset, getStep, getState, getProject
- renommer getCurrentsStatus -> getCurrentStatuses (renvoie tous les steps)
- créer getCurrentStatus -> renvoie un seul step en particulier
